{
  "name": "@rainbot/db",
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate && node src/migrate-pg-boss.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "drizzle-orm": "latest",
    "pg-boss": "^12.30.0",
    "postgres": "latest"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "drizzle-kit": "latest",
    "typescript": "^6.0.0",
    "vitest": "latest"
  }
}
